# AuroraApp AI 开发约束

本文件适用于整个仓库，供 Codex 及其他参与本项目的编码 AI 使用。除非用户在当前任务中明确给出更高优先级的指令，否则必须遵守本文件。

文中的“必须”“不得”和“仅允许”是强制规则；“应”表示没有充分理由时必须遵循。发现规则冲突、信息不足或无法验证时，停止扩大改动，并向用户说明。

## 1. 指令与事实来源

按以下优先级执行：

1. 用户在当前任务中的明确要求。
2. 本文件及目标目录中更具体的 `AGENTS.md` 或 `AGENTS.override.md`。
3. `Documents/Architecture/` 中已接受的架构方案与阶段计划。
4. 仓库内现有接口、测试、配置、代码风格和文档。

不得用推测覆盖已有事实，不得虚构需求、接口、测试结果、性能指标、兼容范围或安全能力。

涉及架构、公开契约、安全边界、持久化语义或不可逆操作的必要信息缺失时，必须暂停实现。一次只向用户提出一个问题，解释问题、各选项的影响和推荐项；问题全部解决后，明确确认不存在未决问题。

## 2. 任务授权边界

开始前先判断任务类型，并严格控制动作范围：

- **解释、评审、检查或诊断**：默认只读。可以检查文件、运行非破坏性命令和测试，但不得修改文件、依赖、配置、分支或外部状态。
- **问题修复**：只修改重现问题、消除已确认根因和补充回归验证所必需的最小范围。不得借修复增加新功能、重做架构或顺手处理无关问题。
- **功能开发或重构**：只实现用户明确要求的行为。不得自行扩展产品范围、平台范围、协议能力或交付阶段。
- **提交、推送、建分支、发布或部署**：只有用户明确要求时才允许执行；对一次任务的授权不得自动沿用到后续任务。

发现相邻缺陷时，应在交付说明中报告；除非它会阻止当前任务完成或导致当前改动不安全，否则不得顺带修改。

## 3. 修改范围与最小变更原则

修改前必须先阅读目标文件、直接调用方、相关测试和适用文档，确认现有约定后再动手。

必须：

- 只改与任务直接相关的文件和代码行。
- 优先修复根因，保留现有公开行为和兼容性。
- 复用现有模块、类型、工具和约定，避免无需求支撑的抽象层。
- 保留用户已有的未提交改动；发生重叠时先识别差异，不得覆盖或回退。
- 将临时文件和构建输出放在仓库规定的位置；`Builds/` 中除说明文件外的产物不得提交。

未经用户明确批准，不得：

- 大范围重命名、移动目录、统一格式化或无关重构。
- 新增生产依赖、升级工具链或依赖主版本、替换框架。
- 改变公开 API、Protobuf/WIT/Schema、包格式、存储结构或兼容策略。
- 创建破坏性数据库迁移，改变备份、恢复、回滚或数据保留语义。
- 改变认证、授权、签名、证书、Secret、审计或功能安全边界。
- 修改 CI/CD、发布流程、许可证、代码所有权或部署基础设施。
- 生成大批脚手架、示例、模拟实现或占位文件。

自动生成文件只能通过仓库内明确的生成器更新，不得手工修改。仓库尚无生成器时，必须先获得用户对生成方式和产物范围的确认。

## 4. 问题修复规则

修复问题时必须形成可追溯链路：现象或失败证据 -> 根因 -> 最小修复 -> 回归验证。

- 能重现时先重现；不能重现时说明限制，并使用最接近的静态证据或定向测试验证假设。
- 优先增加能在修复前失败、修复后通过的回归测试；不适用时说明原因。
- 不得仅处理表面症状而保留已知根因。
- 不得通过吞掉异常、返回伪造成功、静默降级、无限重试或扩大超时掩盖问题。
- 不得删除、跳过或放宽测试来获得通过结果。
- 不得全局关闭编译器、lint、类型检查或安全规则；确需局部抑制时，必须限制到最小范围并说明原因。
- 不得在没有测量或需求依据时以“性能优化”为由改变语义。

如果根因修复必然触发第 3 节中的审批项，只能先给出修复方案、影响范围和迁移/回滚办法，获得确认后再实施。

## 5. Aurora 架构硬约束

实现必须遵守 `Documents/Architecture/Aurora-Platform-Architecture.md`、`Aurora-Runtime-Architecture.md` 和 `Aurora-Delivery-Roadmap.md`。R-001 至 R-067 是已接受基线；任何改变这些边界的提案必须新增 ADR 并重新评审，不得作为实现细节暗中修改。

以下规则尤其不得违反：

- 首版 Runtime 目标仅为普通 Linux x64 与 Rust `std`。不得宣称或预留 RT-Linux、PREEMPT_RT、RTOS、裸机、`no_std` 或硬实时兼容能力。
- 周期控制路径不得直接执行数据库、Redis、DNS、HTTP、网络访问、阻塞文件 I/O、阻塞日志、无界队列、运行期插件发现或不可界定的堆增长。
- 周期路径必须使用固定容量、预分配和有界非阻塞通信；所有循环和工作量必须有可证明上界。
- I/O Guardian 独占物理 I/O 控制权；Control Engine 只能访问 I/O 映像，不得通过 PLC 地址或其他语法绕过 Guardian。
- Aurora ST 是自定义方言，通过工程机或 CI AOT 构建；不得引入 Online Change 或跨版本进程内状态恢复承诺。
- Cyclic Workflow 是具有 PLC 扫描语义和时序展示的可视化控制语言，不得退化成 Hosted Workflow，也不得改成触点/线圈梯形图表示。
- Hosted Workflow 与 Cyclic Workflow 必须保持执行、数据容量、故障和恢复语义隔离。
- 应用采用完整镜像 A/B。Target Agent 位于应用槽外；Runtime Supervisor 位于槽内且无特权。不得削弱断电恢复、签名校验、防回退和 `Fallback` 流程。
- Storage Service 是持久化的唯一数据库入口；PostgreSQL 是持久来源，Redis 只能承载可重建数据。不得让存储背压进入周期线程。
- Gateway 断开不得停止周期控制；本机 HMI 不得依赖 Gateway 才能工作。
- Runtime、HMI、Gateway、ST、工作流和 `Fallback` 均不是功能安全系统。急停、人员防护和危险运动联锁必须由独立安全系统完成，Aurora 不得绕过或替代它。
- 性能结论必须来自指定硬件和工程的测量，至少说明周期、抖动、deadline miss、队列水位和负载条件；不得泛化为平台级保证。

阶段实现必须遵循路线图依赖。允许为后续阶段定义稳定契约或测试替身，但不得提前引入会改变当前阶段边界的完整产品能力。

## 6. 代码生成与设计要求

- 标识符、类型和模块名使用清晰的英文；避免含糊缩写、无语义数字和通用的 `Manager`/`Helper` 堆积。
- 先选择能满足当前需求的最简单设计，不为假设中的未来需求建立框架。
- 公开契约采用显式类型、版本和错误语义；不得用隐藏 UI 状态或进程内偶然行为充当工程语义。
- 时间、大小、速率、地址、字节序、位序和容量必须使用明确单位或类型表达，禁止依赖含糊默认值。
- 并发、跨进程通信和状态机必须明确所有权、顺序、超时、取消、背压、幂等和崩溃恢复行为。
- 有界队列溢出、deadline miss、数据缺口和降级必须可观测，不得静默丢失。
- 解析外部输入时必须校验长度、范围、版本、权限和资源预算；不信任网络、包、插件、设备或工程文件输入。
- 不得将密钥、令牌、证书私钥或个人数据写入源码、普通配置、命令行、环境变量、日志、测试快照或提交历史。
- 不得以占位实现冒充完成。`TODO`、`NotImplemented`、固定假数据和无条件成功返回只能在用户明确接受的原型范围内存在，并必须标明限制。

### 6.1 固定仓库结构

新增代码必须放入以下边界，不得在仓库根目录或自创的同义目录散放项目：

```text
Sources/
  Rust/                 Runtime、Target、Gateway、构建工具和 Rust SDK
    crates/             Rust library crates
    apps/               最终可执行程序入口
  DotNet/               HMI、Studio、.NET SDK 和公共核心库
  Contracts/            proto、wit、schema、control 契约源文件
  Sdk/                  对外 SDK、模板和示例
Documents/
  Architecture/         已接受架构基线
  ADR/                  架构决策记录
  Protocols/            协议语义、兼容矩阵和黄金样本说明
Tools/
  CodeGen/              可重现的代码生成工具
  Packaging/            包、签名信封和 SBOM 工具
  DevEnvironment/       本地与 CI 共用的验证入口
Builds/                 不提交的构建输出
```

- 只有当前交付阶段需要的项目才允许加入 workspace/solution；不得一次生成所有未来产品的空壳。
- Rust crate 名统一使用 `aurora-*` kebab-case；目录名必须与 package 名一致。
- .NET 项目、程序集和根命名空间统一使用 `Aurora.*` PascalCase；项目目录必须与项目名一致。
- 可执行入口只负责组合与生命周期，业务逻辑必须位于可测试的 library/core 项目。
- `Sources/Contracts/` 保存契约源文件；生成代码写入明确的生成目录且禁止手改。
- `Sources/Sdk/` 只能依赖公开稳定契约，不得反向引用 Runtime、HMI 或 Studio 内部实现。
- `Aurora.Studio.Core` 不得引用 WinUI；`Aurora.Hmi.Core` 不得引用 Avalonia。禁止创建同时引用 WinUI 与 Avalonia 的公共 UI 项目。
- host-only 的 `aurora-build`、`aurora-cli` 和生成工具不得成为 Target Runtime 的依赖。

依赖方向必须由外向内、无循环：平台/进程入口 -> 领域实现 -> contracts/types。`aurora-types` 必须保持最小且无 I/O、网络、存储、UI 和进程依赖；contracts 不得依赖具体服务实现。跨进程交互必须经版本化契约，不得直接引用对方内部类型。

### 6.2 Rust 语法与命名

- 以根目录 `rust-toolchain.toml`、`rustfmt.toml` 和 `Sources/Rust/Cargo.toml` 为唯一产品语言与 lint 基线；生产构建禁止 nightly、unstable feature 和未批准的 target。只有 Accepted ADR 明确限定的 host-only 验证工具可使用按日期固定的 nightly（当前仅 LLVM branch coverage），不得启用产品源码 feature 或产出可发布二进制。
- 使用 Rust 2024 edition。crate 必须继承 `[workspace.package]` 和 `[workspace.lints]`，不得自行降低 lint 等级。
- module、function、variable 使用 `snake_case`；type、trait、enum variant 使用 `UpperCamelCase`；constant/static 使用 `SCREAMING_SNAKE_CASE`。
- 文件名与 module 名一致。默认使用 `module.rs`，不得同时混用 `module.rs` 与 `module/mod.rs` 两种布局。
- 表示单位或领域含义的值必须使用 newtype 或明确类型，不得在公开 API 中传递含义不明的裸整数/字符串。
- 公开 API 不得暴露 `Box<dyn Error>` 或仅字符串错误；必须使用可穷举、可测试的领域错误类型。
- 生产代码禁止 `unwrap`、`expect`、`panic!`、`todo!`、`unimplemented!`、`dbg!` 和直接 `print!/println!/eprint!/eprintln!`。
- 默认禁止 `unsafe`。只有无法用安全 Rust 满足且有测量/平台依据时，才可在获得明确审批后局部启用，并满足第 7 节的 `SAFETY` 说明和专门测试。
- 不得在周期路径使用隐式分配、无界集合或隐藏阻塞的便利 API；类型和函数命名应显式体现 bounded/fixed-capacity 语义。

### 6.3 C#/.NET 语法与命名

- 以根目录 `global.json`、`Directory.Build.props` 和 `.editorconfig` 为唯一 SDK、语言和代码风基线；禁止 preview language feature。
- 使用启用 nullable reference types 的 C# 14；不得用 `#nullable disable` 或大范围 warning suppression 绕过检查。
- namespace、type、public/protected member 使用 `PascalCase`；interface 使用 `I` 前缀；parameter/local 使用 `camelCase`；private instance field 使用 `_camelCase`；constant 使用 `PascalCase`。
- 每个文件原则上只包含一个主要 public type，文件名与该类型一致；使用 file-scoped namespace。
- 返回 `Task`/`ValueTask` 且代表异步操作的方法以 `Async` 结尾，并接受/传播适用的 `CancellationToken`。
- 禁止 `async void`（事件处理器除外）、阻塞等待异步任务、空 `catch`、`catch (Exception)` 后静默继续，以及使用 `null!` 掩盖未完成初始化。
- 公共边界优先使用不可变类型和显式结果/异常语义；不得以 `Dictionary<string, object>`、匿名对象或 UI 类型代替稳定契约。
- 平台专用 API 必须留在对应 platform/UI 项目，不得泄漏到 `*.Core` 或 SDK。

## 7. 注释、API 说明与文档

注释用于解释“为什么”、约束和不易观察的语义，不得逐行复述代码。代码变更不得靠注释掩盖过度复杂的实现。

默认规则：

- 代码标识符使用英文；代码注释和项目说明以简体中文为主，标准术语、协议字段和符号保留英文。已有模块形成其他一致约定时遵循现有约定。
- 新增或改变公开 API、协议、配置项、命令、Schema 或用户可见行为时，必须同步更新相应说明。
- 公开 API 文档至少说明用途、输入输出、错误/故障、线程或任务上下文，以及适用时的单位、容量、阻塞、分配、超时、幂等、兼容和安全要求。
- 周期路径中的非显然代码必须说明执行上界、容量假设、溢出/越界语义和为何不会阻塞或动态增长。
- Rust `unsafe` 块必须紧邻 `// SAFETY:` 说明并列出调用方必须维持的不变量；不能证明安全时不得使用 `unsafe`。
- 复杂状态机必须记录状态、触发条件、守卫、持久化点、失败转移和恢复路径。
- `TODO`/`FIXME` 必须包含具体原因、完成条件，以及可用时的 issue 标识；禁止“以后优化”之类无行动条件的注释。
- 删除或改正已失真的注释，禁止保留与实现不一致的说明。

仅修复内部实现且不改变公开行为时，不为凑数量创建无价值文档。架构文档只能在架构或契约确实变化并已获批准时更新。

## 8. 测试与验证门槛

所有生产代码、公开契约和生成器变更都必须有与风险相称的自动化验证。

- 至少运行目标模块的格式化、静态检查、单元测试和相关集成测试；仓库提供统一脚本后优先使用统一脚本。
- 修复必须覆盖原始失败场景以及关键边界条件。
- 契约或序列化变更必须验证向前/向后兼容策略、未知字段和版本拒绝路径。
- 周期控制必须验证确定性、容量/边界、deadline、队列溢出、Fault 和 `Fallback` 行为。
- 部署、A/B、审计和持久化状态机必须包含崩溃、断电、重复请求、空间不足和恢复测试。
- 安全相关改动必须包含拒绝路径、最小权限和敏感信息不泄漏验证。
- UI 改动必须在目标最低分辨率 1920x1080 下验证，并覆盖更大窗口和关键缩放场景；不得只凭源代码判断布局正确。
- 不得声称未运行的测试通过。因环境、硬件或依赖无法执行时，交付说明必须列出未执行项、原因和剩余风险。

测试结果有失败时，不得把既有失败直接归因于用户或环境。先判断它是否与当前改动有关，并提供证据。

## 9. 依赖、兼容与安全

- 新依赖必须有明确必要性；在获得批准前只能提出候选，不得安装或写入锁文件。
- 获批新增依赖时，记录用途、许可证、维护状态、供应链风险、替代方案和版本锁定策略。
- Stable 契约不得直接破坏；Preview 契约的破坏性修改也必须提供迁移路径。
- 共享内存布局、Protobuf/WIT/Schema 和包清单必须版本化，并有兼容矩阵和黄金样本测试。
- 外部命令、文件删除、迁移、部署和设备控制必须精确限定目标。禁止对仓库根目录、用户目录或未解析变量执行递归破坏操作。
- 不得为了让流程通过而关闭签名、证书校验、授权、审计、防回退或输入验证。

## 10. Git 与交付说明

- 不得使用 `git reset --hard`、`git clean -fd`、`git checkout -- <file>` 或等效方式丢弃用户改动。
- 不得修改与任务无关的未提交文件，不得擅自改写历史、变基、合并、打标签、推送或发布。
- 提交前必须检查 `git diff`、`git diff --check` 和仓库状态，确保没有临时文件、Secret 或无关改动。
- 提交信息应说明意图而不是罗列文件；只有用户明确要求时才创建提交。

最终交付必须清楚说明：

1. 实际改变了什么，以及未改变什么。
2. 修改的文件和关键设计选择。
3. 已运行的验证及结果。
4. 未运行的验证、已知限制和剩余风险。
5. 是否需要迁移、部署步骤、人工确认或后续决策。

## 11. 完成定义

只有同时满足以下条件，才可以宣称任务完成：

- 用户要求的行为已经实现，没有用占位逻辑替代。
- 改动没有越过授权范围和已接受架构边界。
- 相关代码、注释、契约和文档彼此一致。
- 必要测试已经补充并通过，或未执行项已如实说明。
- 工作区中没有本任务产生的临时文件、构建产物、Secret 或无关修改。
- 交付说明能够让另一名开发者复核、复现和继续工作。

## Code Review Rules

审查代码时，优先报告可导致错误结果、数据丢失、失控输出、安全边界破坏、死锁/阻塞、无界资源使用、协议不兼容或无法恢复的问题。每个问题必须指出具体位置、触发条件和影响；可以确定安全修复路径时一并给出。不要把纯格式偏好当作缺陷，也不要声称没有证据支持的风险已经发生。
